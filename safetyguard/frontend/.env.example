# Copy to .env.local (Next.js reads .env.local automatically)
# Point at your SafetyGuard API (same machine default below).

NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_WS_URL=ws://localhost:8000
