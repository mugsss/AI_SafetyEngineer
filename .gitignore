# Local SQLite DB sometimes created at repo root during dev
/safetyguard.db

# Code graph RAG (Chroma per-run)
/safetyguard/backend/code_index/
/safetyguard/backend/vendor/

# Legacy local git clones (default is now under system temp)
/safetyguard/backend/repos/

# Editable-install metadata (regenerated by pip install -e .)
*.egg-info/
